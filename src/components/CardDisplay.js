import React, { useEffect, useState } from "react";
import axios from "axios";
import Card from "./card";

export default function CardDisplay({ lmit }) {
  const [cardInfo, setCardInfo] = useState({});
}
return <div></div>;
